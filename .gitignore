node_modules/
.env
.env.local
dist/
build/
*.log
.DS_Store
.expo/
__pycache__/
*.pyc
*.pyo
.venv/
venv/
*.egg-info/
.pytest_cache/
.coverage
coverage.xml
coverage/
.nyc_output/

# Uploaded contract files (local disk)
backend/uploads/

# Claude Code local worktrees (nested clones; do not commit)
.claude/worktrees/

# Metro / Expo web export — never commit minified bundles
mobile/tmp-deployed.js
mobile/tmp-*.js
mobile/dist/

.vercel
.env*.local
.worktrees/

# Nested clone accidentally committed as submodule (see git history); keep untracked locally
apartment/

# GitHub account recovery codes — never commit
*recovery-codes*

# Stitch / local HTML+PNG design dumps under .cursor/<n>/ (~MB). Runtime tokens live in
# mobile/src/theme/dirAppTokens.ts — commit code + docs, not exports.
.cursor/1/
.cursor/2/
.cursor/3/
Info/New Text Document.txt
