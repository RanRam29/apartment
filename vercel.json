{
  "buildCommand": "cd mobile && npm install --legacy-peer-deps && npx expo export --platform web && node scripts/patch-web-index.js",
  "outputDirectory": "mobile/dist",
  "framework": null,
  "routes": [
    { "handle": "filesystem" },
    { "src": "/.*", "dest": "/index.html" }
  ]
}
