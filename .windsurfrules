# You are Agent CASCADE — Identity + Platform workstream

## First Steps
1. Read `AGENTS.md` in this project root — it contains your full task assignment
2. Read the implementation plan at `docs/superpowers/plans/2026-05-27-dirapp-mvp-v3.md` — it has exact code for each task
3. You are on branch `wind/identity-platform` — commit all work to this branch

## Your Tasks (in order)
- T2: Notification System v2 — Resend Email + unified notify service
- T3: Terms of Service — middleware + acceptance endpoint
- T6: KYC v2 — Persona abstraction, HMAC webhook, ID checksum
- T14: Multi-tenant — role switch, blocking, account locking
- T12: Maintenance Flow — tickets, invoices, midrag.co.il link
- T13: Guarantor Web Flow — invite, web SPA, Persona Web SDK
- T17: Mobile Screens — all new screens + Zustand stores

## Status Reporting
**IMPORTANT:** After completing each task, update `STATUS.md` in this project root:
1. Move the completed task from "Current Task" to the "Completed Tasks" table
2. Set your next task as "Current Task"
3. List all files you created/modified
4. Log any blockers immediately
5. Update the "Last Updated" timestamp

## Critical Rules
- ONLY touch files listed under your section in AGENTS.md
- Never modify files owned by other agents
- For shared files (app.js, models/index.js, package.json) — only ADD lines at the end, never remove or reorder
- All new endpoints use `/api/v3/` prefix
- Follow existing code patterns — look at `backend/src/routes/auth.js` for style
- Commit after each subtask
- Run `cd backend && npx jest --no-coverage` before marking a task done
- Update task status in AGENTS.md when you complete a task (🔴 → 🟢)

## Tech Context
- Backend: Node.js / Express / Sequelize (PostgreSQL) / Jest
- Mobile: React Native / Expo 53 / Zustand / TypeScript
- This is an Israeli rental platform — some content is in Hebrew
- Existing models are in `backend/src/models/pg/` — follow the same UUID + timestamps pattern
- Auth middleware: `const { authenticate, requireRole } = require('../middleware/auth')`
- Existing mobile screens are in `mobile/src/screens/` — follow the same pattern
- Existing Zustand stores are in `mobile/src/store/` — follow useAuthStore.ts pattern
